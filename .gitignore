# Công cụ - KHÔNG đưa vào repo (file rất nặng)
claude.exe

# Cấu hình editor cá nhân
.vscode/

# File hệ thống
Thumbs.db
.DS_Store

# Agent skills (công cụ hỗ trợ, không thuộc web)
.agents/
skills-lock.json
